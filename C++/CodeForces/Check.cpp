# include <bits/stdc++.h>
using namespace std ;

int main() {
    #ifndef ONLINE_JUDGE
    freopen("input.txt","r",stdin);
    freopen("output.txt","w",stdout) ;
    #endif
    // char c ;
    for(int i = 0 ; i < 3 ; i++) {
        char c ;
        cin >> c ;
        if ( c == 'B') 
            cout << "True" ;
    }
}
