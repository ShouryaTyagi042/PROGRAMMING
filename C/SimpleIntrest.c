#include <stdio.h>

int main() {
    int p , r ,t ;
    scanf("%d %d %d",&p,&r,&t);
    float interest ;
    interest = (p*r*t) / 100 ;
    printf("%.3f ",interest);
    return 0;
}
//2 programs
// find the largest using if else
// give the grade of the student
